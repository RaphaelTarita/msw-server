syntax = "proto3";

package msw.server.rpc;

service RouteService {
  rpc getRouteNote(Point) returns (RouteNote) {}
}

message Point {
  int32 latitude = 1;
  int32 longitude = 2;
}

message RouteNote {
  string msg = 1;
}